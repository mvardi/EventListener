import XCTest
@testable import EventListenerSDK

final class EventListenerSDKTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(EventListenerSDK().text, "Hello, World!")
    }
}
